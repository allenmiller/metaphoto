version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - ls -al
      - env
      - npm install
      - npm install -g serverless
  build:
    commands:
      - env
      - serverless deploy -v
