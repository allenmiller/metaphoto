version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install -g serverless
      - npm install serverless-domain-manager
  build:
    commands:
      - cd services/time
      - export SLS_DEBUG=1
      - serverless deploy
