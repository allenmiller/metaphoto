service: metaphoto-time

provider:
  name: aws
  runtime: nodejs10.x
  stage: dev
  region: us-west-1
  deploymentBucket: ${cf:metaphoto-us-west-1-dev.MetaphotoPipelineArtifactsBucket}

plugins:
  - serverless-domain-manager

custom:
  stage: ${opt:stage, self:provider.stage}
  customDomain:
    domainName: ${cf.metaphoto-us-west-1-dev.MetaphotoHostedZone}
    stage: ${self:provider.stage}
    basePath: ${self:provider.stage}
    certificateName: ${cf.metaphoto-us-west-1-dev.MetaphotoHostedZone}
    endpointType: REGIONAL
    createRoute53Record: true

functions:
  get:
    handler: time.handler
    events:
      - http:
          path: time
          method: get
          cors: true
